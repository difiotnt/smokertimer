# Smokertimer Android

Folder Android siap-build ada di `smokertimer-android/`.

Build APK debug:

```bash
cd smokertimer-android
./gradlew assembleDebug
```

Kalau workflow GitHub Actions dipakai, artifact APK akan muncul dari:

`smokertimer-android/app/build/outputs/apk/debug/app-debug.apk`
